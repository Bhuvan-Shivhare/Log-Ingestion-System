apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
type: Opaque
stringData:
  # ⚠️ REPLACE WITH REAL CONNECTION STRING BEFORE APPLYING ⚠️
  # Example: mongodb+srv://user:pass@cluster.mongodb.net/dbname
  MONGO_URL: "<PLACEHOLDER_FOR_REAL_MONGO_URL>"
