# Backend Configuration
PORT=3000

# Database Connection
# ------------------------------------------------------------------
# OPTION 1: Local Docker Development (Default)
# Uses the 'mongo' service defined in docker-compose.yml
# MONGO_URI=mongodb://mongo:27017/logs

# OPTION 2: MongoDB Atlas (Cloud)
# Uncomment and replace with your actual connection string.
# DO NOT COMMIT REAL CREDENTIALS TO GIT!
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.abcde.mongodb.net/logs?retryWrites=true&w=majority

# Redis Configuration
# For Docker Compose, use the service name 'redis'
REDIS_URL=redis://redis:6379
